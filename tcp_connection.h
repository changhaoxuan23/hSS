#ifndef TCP_CONNECTION_H_
#define TCP_CONNECTION_H_
#include <common.h>
// initialize the underlying information of a connection
void tcp_initialize_underlying(struct connection_information *connection);
#endif